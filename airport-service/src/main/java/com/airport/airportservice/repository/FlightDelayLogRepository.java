package com.airport.airportservice.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FlightDelayLogRepository extends JpaRepository<FlightDelayLogRepository, Long> {
}
